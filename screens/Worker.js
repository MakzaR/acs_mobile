import React, {useState} from 'react';
import {StyleSheet, View, Text, Button} from 'react-native';

export default function Worker({navigation}) {
    return (
        <View>
            <Text>WorkerInfo</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});